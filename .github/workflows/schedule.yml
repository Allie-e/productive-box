name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: ${{ secrets.https://gist.github.com/wooyani77 }}
          TIMEZONE: Asia/Seoul
